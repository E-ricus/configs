#!/usr/bin/env bash
# Bootstrap script for post-installation setup
# Works on NixOS, macOS, and other Linux distributions
# Usage:
#   Download first: curl -fsSL https://raw.githubusercontent.com/e-ricus/.dotfiles/main/bootstrap/1 > setup.sh
#   Then run: bash setup.sh

set -euo pipefail

# Source shared functions
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ -f "$SCRIPT_DIR/lib.sh" ]; then
    source "$SCRIPT_DIR/lib.sh"
else
    # If running from curl, download lib.sh
    eval "$(curl -fsSL https://raw.githubusercontent.com/e-ricus/.dotfiles/main/bootstrap/lib.sh)"
fi

DOTFILES_DIR="$HOME/.dotfiles"

setup_nixos() {
    log_info "=== Setting up NixOS ==="

    # For NixOS post-install, we're running as root
    DOTFILES_DIR="/root/.dotfiles"

    # Check if dotfiles exist, if not clone
    if [ ! -d "$DOTFILES_DIR" ]; then
        log_info "Cloning dotfiles repository..."
        ensure_git
        clone_dotfiles "$DOTFILES_DIR"
    else
        log_success "Dotfiles already exist at $DOTFILES_DIR"
    fi

    # Prompt user for architecture/system selection
    echo
    log_info "Select your system configuration:"
    echo "  1) x86_64 (Intel/AMD 64-bit)"
    echo "  2) aarch64 (ARM 64-bit)"
    echo

    local flake_config=""
    local hw_config_dest=""
    while true; do
        read -p "Enter selection (1 or 2): " arch_selection
        case $arch_selection in
            1)
                flake_config="nixos-x86"
                hw_config_dest="$DOTFILES_DIR/nix/systems/nixos/x86_64/hardware-configuration.nix"
                break
                ;;
            2)
                flake_config="nixos-arm"
                hw_config_dest="$DOTFILES_DIR/nix/systems/nixos/aarch64/hardware-configuration.nix"
                break
                ;;
            *)
                log_error "Invalid selection. Please enter 1 or 2."
                ;;
        esac
    done

    log_info "Using flake configuration: $flake_config"
    echo

    # Copy hardware-configuration.nix from /etc/nixos to dotfiles
    log_info "Copying hardware-configuration.nix to dotfiles..."
    if [ -f /etc/nixos/hardware-configuration.nix ]; then
        # Ensure destination directory exists
        mkdir -p "$(dirname "$hw_config_dest")"
        cp /etc/nixos/hardware-configuration.nix "$hw_config_dest"
        log_success "Hardware configuration copied to dotfiles"
    else
        log_error "Hardware configuration not found at /etc/nixos/hardware-configuration.nix"
        log_error "Cannot proceed without hardware configuration"
        exit 1
    fi

    echo
    log_info "Hardware configuration is now in:"
    log_info "  $hw_config_dest"
    echo

    # Rebuild using flake
    log_info "Rebuilding NixOS from flake..."
    log_info "This may take several minutes on first run..."
    echo

    cd "$DOTFILES_DIR/nix"
    if nixos-rebuild switch --flake ".#$flake_config"; then
        log_success "NixOS rebuilt successfully with flake!"
        log_info "Note: home-manager is integrated in the system rebuild"
    else
        log_error "Failed to rebuild NixOS!"
        log_info "You can try manually with:"
        log_info "  cd $DOTFILES_DIR/nix && nixos-rebuild switch --flake .#$flake_config"
        exit 1
    fi

    # Set user password
    echo
    log_info "Setting password for user 'ericus'..."
    if passwd ericus; then
        log_success "User password set successfully"
    else
        log_warning "Failed to set user password. You can set it later with: passwd ericus"
    fi

    # Clone dotfiles to user's home and set permissions
    echo
    log_info "Setting up dotfiles in user home directory..."

    USER_DOTFILES="/home/ericus/.dotfiles"
    if [ ! -d "$USER_DOTFILES" ]; then
        log_info "Cloning dotfiles to /home/ericus/.dotfiles..."
        git clone https://github.com/e-ricus/.dotfiles.git "$USER_DOTFILES"

        # Set ownership to user
        chown -R ericus:users "$USER_DOTFILES"
        log_success "Dotfiles cloned and ownership set to ericus"
    else
        log_info "Dotfiles already exist at $USER_DOTFILES"
        # Ensure correct ownership anyway
        chown -R ericus:users "$USER_DOTFILES"
    fi

    # Clean up root's dotfiles
    if [ -d "/root/.dotfiles" ]; then
        log_info "Cleaning up /root/.dotfiles..."
        rm -rf "/root/.dotfiles"
        log_success "Root dotfiles cleaned up"
    fi

    echo
    log_success "Setup complete! Dotfiles are ready at /home/ericus/.dotfiles"
    log_info "When you login as ericus, you can use the 'hm' and 'nos' aliases"
}

setup_darwin() {
    log_info "=== Setting up macOS with nix-darwin ==="

    # Check if Nix is installed
    if ! command_exists nix; then
        log_error "Nix is not installed!"
        echo
        log_info "Please install Nix first using the Determinate Systems installer:"
        log_info "  curl --proto '=https' --tlsv1.2 -sSf -L https://install.determinate.systems/nix | sh -s -- install"
        echo
        log_info "After installing Nix, restart your terminal and run this script again."
        exit 1
    fi

    log_success "Nix is installed"

    # Ensure git
    ensure_git

    # Clone dotfiles if needed
    if [ ! -d "$DOTFILES_DIR" ]; then
        log_info "Cloning dotfiles to $DOTFILES_DIR..."
        clone_dotfiles "$DOTFILES_DIR"
    else
        log_success "Dotfiles already exist at $DOTFILES_DIR"
    fi

    # Check if unified flake exists
    if [ ! -f "$DOTFILES_DIR/nix/flake.nix" ]; then
        log_error "Unified flake not found!"
        log_error "Expected: $DOTFILES_DIR/nix/flake.nix"
        exit 1
    fi

    # Install nix-darwin with unified flake
    log_info "Installing nix-darwin with unified flake..."
    log_info "This may take several minutes on first run..."
    echo

    cd "$DOTFILES_DIR/nix"
    if sudo nix run nix-darwin/master#darwin-rebuild -- switch --flake ".#work-mac"; then
        log_success "nix-darwin installed and configured"
        log_info "Note: home-manager is integrated in the darwin rebuild"
    else
        log_error "Failed to install nix-darwin!"
        log_info "You can try manually with:"
        log_info "  cd ~/.dotfiles/nix && sudo nix run nix-darwin/master#darwin-rebuild -- switch --flake .#work-mac"
        exit 1
    fi
}

setup_linux() {
    # TODO: Implement setup for other Linux distributions
    # This should:
    #   1. Check if Nix is installed, if not fail and prompt to install with Determinate Systems
    #   2. Ensure git is installed
    #   3. Clone dotfiles repository if not present
    #   4. Set up standalone home-manager configuration
    #   5. Apply home-manager switch --flake ~/.dotfiles/nix#ericus

    log_error "Other Linux distributions are not yet supported in this bootstrap script"
    log_info "Please install Nix manually using Determinate Systems installer:"
    log_info "  curl --proto '=https' --tlsv1.2 -sSf -L https://install.determinate.systems/nix | sh -s -- install"
    exit 1
}

setup_home_manager() {
    local system=$(detect_system)

    # For NixOS and Darwin, home-manager is integrated in system rebuild
    if [ "$system" = "nixos" ] || [ "$system" = "darwin" ]; then
        log_info "=== Home-manager setup ==="
        log_success "home-manager is already integrated in your system configuration"
        log_info "It was included in the system rebuild"
        echo
        log_info "Optional: You can also use standalone home-manager for quick iterations:"
        log_info "  home-manager switch --flake ~/.dotfiles/nix#$(get_hm_user)"
        echo
        return 0
    fi

    # For regular Linux, set up standalone home-manager
    log_info "=== Setting up standalone home-manager ==="

    # Check if unified flake exists
    if [ ! -f "$DOTFILES_DIR/nix/flake.nix" ]; then
        log_error "Unified flake not found at $DOTFILES_DIR/nix/flake.nix"
        log_error "Cannot proceed with home-manager setup"
        return 1
    fi

    # Determine which user configuration to use
    local hm_user=$(get_hm_user)
    log_info "Using home-manager configuration for: $hm_user"

    # Check if the user configuration exists
    local user_config=""
    case $system in
        linux)
            user_config="$DOTFILES_DIR/nix/home/linux.nix"
            ;;
    esac

    if [ ! -f "$user_config" ]; then
        log_error "User configuration not found: $user_config"
        return 1
    fi

    # Apply home-manager configuration directly from unified flake
    log_info "Applying home-manager configuration..."
    log_info "This may take several minutes on first run..."
    echo

    if home-manager switch --flake "$DOTFILES_DIR/nix#$hm_user"; then
        log_success "home-manager configured successfully"
    else
        log_error "home-manager switch failed!"
        log_info "You can try manually with: home-manager switch --flake ~/.dotfiles/nix#$hm_user"
        return 1
    fi
}

setup_symlinks() {
    log_info "=== Setting up dotfile symlinks ==="
    
    # Verify symlinkmanager exists
    if [ ! -x "$DOTFILES_DIR/bin/symlinkmanager" ]; then
        log_error "symlinkmanager not found at $DOTFILES_DIR/bin/symlinkmanager"
        log_error "Cannot proceed with dotfile symlinking"
        return 1
    fi
    
    # Verify symlink.conf exists
    if [ ! -f "$DOTFILES_DIR/symlink.conf" ]; then
        log_error "symlink.conf not found at $DOTFILES_DIR/symlink.conf"
        log_error "Cannot proceed with dotfile symlinking"
        return 1
    fi
    
    # Run symlinkmanager
    stow_configs "$DOTFILES_DIR"
    
    log_success "All dotfiles symlinked"
}

main() {
    echo
    log_info "╔════════════════════════════════════════╗"
    log_info "║   Dotfiles Bootstrap - Post Install   ║"
    log_info "╚════════════════════════════════════════╝"
    echo
    
    # Detect system
    SYSTEM=$(detect_system)
    log_info "Detected system: $SYSTEM"
    echo
    
    # System-specific setup
    case $SYSTEM in
        nixos)
            setup_nixos
            ;;
        darwin)
            if ! setup_darwin; then
                log_warning "Darwin setup failed or was skipped"
                log_info "Continuing with home-manager setup..."
            fi
            ;;
        linux)
            setup_linux
            ;;
        *)
            log_error "Unsupported system type: $SYSTEM"
            exit 1
            ;;
    esac
    
    echo

    # Setup home-manager (integrated for NixOS/Darwin, standalone for Linux)
    if ! setup_home_manager; then
        log_error "home-manager setup failed!"
        log_warning "You may need to fix configuration errors and run manually"
        echo
        log_info "After fixing, run: home-manager switch --flake ~/.dotfiles/nix#$(get_hm_user)"
        exit 1
    fi
    
    echo
    
    # Setup dotfile symlinks (common for all systems)
    if ! setup_symlinks; then
        log_error "Dotfile symlinking failed!"
        log_warning "You may need to run symlinkmanager manually"
        echo
        log_info "To fix, run: cd ~/.dotfiles && bin/symlinkmanager link all"
    fi
    
    echo
    log_success "╔════════════════════════════════════════╗"
    log_success "║     Bootstrap Complete! 🎉             ║"
    log_success "╚════════════════════════════════════════╝"
    echo
    
    log_info "Your dotfiles are now installed and configured."
    log_info "Dotfiles location: $DOTFILES_DIR"
    log_info "Unified flake: ~/.dotfiles/nix/flake.nix"
    echo

    log_info "Next steps:"
    case $SYSTEM in
        nixos)
            log_info "• Your system is ready to use!"
            log_info "• Start Hyprland with: Hyprland"
            echo
            log_info "Commands (or use aliases):"
            log_info "• System rebuild (includes home-manager):"
            log_info "    sudo nixos-rebuild switch --flake ~/.dotfiles/nix#nixos-x86"
            log_info "    Alias: nos"
            log_info "• Quick home-manager only update:"
            log_info "    home-manager switch --flake ~/.dotfiles/nix#$(get_hm_user)"
            log_info "    Alias: hm"
            ;;
        darwin)
            log_info "• Restart your terminal or run: exec \$SHELL"
            echo
            log_info "Commands (or use aliases):"
            log_info "• System rebuild (includes home-manager):"
            log_info "    darwin-rebuild switch --flake ~/.dotfiles/nix#work-mac"
            log_info "    Alias: nom"
            log_info "• Quick home-manager only update:"
            log_info "    home-manager switch --flake ~/.dotfiles/nix#$(get_hm_user)"
            log_info "    Alias: hm"
            ;;
        linux)
            log_info "• Restart your terminal or run: exec \$SHELL"
            log_info "• To update home-manager:"
            log_info "    home-manager switch --flake ~/.dotfiles/nix#$(get_hm_user)"
            log_info "    Alias: hm"
            ;;
    esac
    echo
    
    # Only offer reboot for fresh installations
    if [ "$SYSTEM" = "nixos" ] && [ ! -f "$HOME/.config/.bootstrap_complete" ]; then
        # Mark as complete
        mkdir -p "$HOME/.config"
        touch "$HOME/.config/.bootstrap_complete"
        
        if confirm "This looks like a fresh install. Reboot now?"; then
            log_info "Rebooting..."
            sudo reboot
        else
            log_info "Remember to reboot or restart your shell for all changes to take effect"
        fi
    else
        log_info "Restart your shell or reboot for all changes to take effect"
    fi
}

main "$@"
