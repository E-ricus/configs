# Everything is ignored except config files.
# Runtime data is redirected to XDG_DATA_HOME / XDG_CACHE_HOME,
# but this is a safety net in case anything slips through.
*
!.gitignore
!init.el
!early-init.el
!local/
!local/**
